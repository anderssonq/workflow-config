# VS Code / Cursor extensions. Install with:
#   xargs -n1 code --install-extension < dotfiles/editors/extensions.txt
#
# Two. The editor is a fallback here; the actual work happens in Neovim and the
# terminal, so the extension list stays short enough to audit.
anthropic.claude-code
vscodevim.vim
